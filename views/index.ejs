<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs', { title: "MoviClaro"}) %>

    <body>
        <%- include('./partials/header.ejs') %>
            <main>
                <div class="home-presentacion">
                    <h1>Bienvenidos a</h1>
                    <img src="/images/MoviClaro.png" alt="logo tipografico oficial">
                </div>
                <div class="main-container">
                    <h2>Ofertas</h2>
                    <!-- Contenido principal -->
                    <section class="conteiner">
                        <!-- Inicio articulo -->
                        <% productsOfertas.forEach(product => { %>
                            <article class="articulo">
                                <form action="/agregarCarrito" method="GET">
                                    <div class="img-container">
                                        <a href="/detalle/<%= product.id %>">
                                        <img class="imagenArticulo" src="/images/products/<%= product.image ? product.image : 'motog20-breeze-blue_front.png' %> " alt="logo">
                                    </a>
                                    </div>
                                    <div class="articulo-desc">
                                        <h3>
                                            <%= product.marca %>
                                        </h3>
                                        <h3>
                                            <%= product.modelo %>
                                        </h3>
                                        <p>
                                            <%= product.color %>
                                        </p>
                                        <p>
                                            <%= product.precio %>
                                        </p>
                                        <a href="/detalle/<%= product.id %>">
                                            <p>Ver detalle</p>
                                        </a>
                                        <input class="botonComprar" value="Comprar" type="submit" href="./agregarCarrito">
                                    </div>
                                </form>
                            </article>
                            <% }); %>
                    </section>
                    <!-- Fin de contenido principal -->
                    </section>
                </div>
                </div>
                <div class="main-container">
                    <h2>Promociones</h2>
                    <!-- Contenido principal -->
                    <section class="conteiner">
                        <!-- Inicio articulo -->
                        <% productsPromoción.forEach(product => { %>
                            <article class="articulo">
                                <form action="/agregarCarrito" method="GET">
                                    <div>
                                        <img class="imagenArticulo" src="/images/products/<%= product.image ? product.image : 'motog20-breeze-blue_front.png' %> " alt="logo">

                                    </div>
                                    <div class="articulo-desc">
                                        <h3>
                                            <%= product.marca %>
                                        </h3>
                                        <h3>
                                            <%= product.modelo %>
                                        </h3>
                                        <p>
                                            <%= product.color %>
                                        </p>
                                        <p>
                                            <%= product.precio %>
                                        </p>
                                        <a href="/detalle/<%= product.id %>">
                                            <p>Ver detalle</p>
                                        </a>
                                        <input class="botonComprar" value="Comprar" type="submit" href="./agregarCarrito">
                                    </div>
                                </form>
                            </article>
                            <% }); %>
                    </section>
                    <!-- Fin de contenido principal -->
                    </section>
                </div>
                </div>


            </main>
            <%- include('./partials/footer.ejs') %>
    </body>

</html>